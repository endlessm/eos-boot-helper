[Unit]
Before=local-fs.target
|ConditionKernelCommandLine=ostree
|ConditionIsMountPoint=/sysroot/boot

[Mount]
What=/sysroot/boot
Where=/boot
Type=none
Options=bind

[Install]
WantedBy=local-fs.target
